{
  "name": "grid-wallpaper",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "Offline interactive Canvas wallpaper for Windows and browsers",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check wallpaper/grid-config.js && node --check wallpaper/grid-wallpaper.js && node --check wallpaper/grid-live-telemetry.js && node --check wallpaper/grid-native-settings.js && node --check wallpaper/grid-settings.js && node scripts/lively-properties.cjs --check && node --test tests/runtime.test.cjs",
    "test:browser": "playwright test",
    "test:install": "set PSModulePath=&& powershell.exe -NoProfile -File tests/install.test.ps1",
    "generate": "node scripts/lively-properties.cjs",
    "thumbnail": "node scripts/thumbnail.cjs",
    "build:settings": "set PSModulePath=&& powershell.exe -NoProfile -File scripts/build-settings.ps1",
    "package": "set PSModulePath=&& powershell.exe -NoProfile -File scripts/package.ps1"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "gifenc": "1.0.3"
  }
}
